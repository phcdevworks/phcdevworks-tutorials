<%= form_with(model: tutorial_category, local: true) do |form| %>
  <% if tutorial_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tutorial_category.errors.count, "error") %> prohibited this tutorial_category from being saved:</h2>

      <ul>
        <% tutorial_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tutorial_category_name %>
    <%= form.text_field :tutorial_category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
